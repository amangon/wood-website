import React from 'react'

// OTP flow is removed in this build (backend supports only email+password).
export const OtpVerificationPage = () => {
  return <div style={{ padding: 24 }}>OTP verification is not available.</div>
}

